#include <iostream>
using namespace std;

int main()
{
    int thang;
    cout << "Nhap thang";
    cin >> thang;
    if (thang > 12 || thang, 1)
    {
        cout << "Thang khong hop le" << endl;
    }
    else if (thang >= 1 && thang <= 3)
    {
        cout << "Qui 1" << endl;
    }
    else if (thang >= 4 && thang <= 6)
    {
        cout << "Qui 2" << endl;
    }
    else if (thang >= 7 && thang <= 9)
    {
        cout << "Qui 3" << endl;
    }
    else
    {
        cout << "Qui 4" << endl;
    }
}